# SG Pathway — Roadmap

This file records what has shipped and what is scheduled. Until now the project
tracked quality (`RATING.md`) but never published a forward plan, which is part
of why two official-language locales were never explicitly scheduled to parity
(see `REVIEW.md`, Severity 1).

## Shipped

| Version | Theme |
| - | - |
| v2.0 | Campaigns, voice narration, smart suggestions |
| v2.1–2.5 | Spaced retrieval, exam/OSCE, case builder, educator tools |
| v3.0 | Exams, authoring studio, educator cohort tools |
| v4.0 | Branching cases, vitals, scenario generator, analytics, sandbox |
| v5.0 | Today's plan, duel mode, specialty cases, high-contrast |
| v6.0 | Timeline scrubber, coach micro-curricula, peer-review threads |
| v7.0 | Competency tier, Brier calibration, handoff tokens, study sets, cheatsheet |
| v8.0 | Weekly goals, bookmarks, flashcards, mastery ladder, printable portfolio |
| v9.0 | Case journal, freeze tokens, calibration trend, best-case certificate, smart debrief |

Throughout, **English and Chinese** were maintained at 100% parity (UI chrome
and all case content), enforced by tests.

## In progress — Official-language parity (Malay + Tamil)

Singapore has four official languages. Malay (the national language) and Tamil
were scaffolded as UI-chrome stubs early on but never built to parity, and the
gap widened with every English/Chinese feature arc. This track closes it.

| Version | Deliverable | Status |
| - | - | - |
| **v9.1** | Honesty + tracking: locale coverage metadata, "partial" switcher hint, CI parity metric with enforced floor, expanded critical-key set, review + this roadmap, README claim qualified | ✅ shipped |
| **v9.2** | **Malay (`ms`) UI chrome → full parity** with English (627/627, enforced) | ✅ shipped |
| **v9.3** | **Tamil (`ta`) UI chrome → full parity** with English (627/627, enforced; native-review-flagged) | ✅ shipped |

(The `ms`/`ta` case-content translation track is paused — see the
Walkthrough arc below, which the user prioritised next. Resume after v10.0.)

## In progress — Visual pathway walkthrough (Bandersnatch-style cinematic)

Scrubbable + branching cinematic of the full STEMI patient journey: 995
collapse → ambulance → ED → cath lab → ward → discharge → rehab → outpatient
review. Hybrid Phaser + embedded MP4 clips for showpiece moments. Includes
atmospheric support staff (AHP, cooks, cleaners, laundry, admin) throughout
plus one dedicated "Back of house" chapter. Lives as a new standalone mode.

| Version | Deliverable | Status |
| - | - | - |
| **v9.4** | **Foundation**: data model, traversal helpers, two starter chapters + branch-point demo, React/SVG renderer with scrubbable timeline, clickable actor cards, branch-decision overlay, top-level launcher, i18n keys in all four locales. | ✅ shipped |
| **v9.5** | **Full STEMI clinical journey + back-of-house chapter**: arrive-SGH (door-to-cath fast-track), cath-activation, diagnostic cath, cardiac CTA (with MRI-yes/no branch), cardiac MRI, family conference (with PCI/CABG branch), surgeon-decline (CABG declined → PCI), PCI procedure, CCU transfer, ward stay with morning round + AHP team, complications + escalation, discharge with case manager + billing, outpatient pharmacy, NHCS Phase II cardiac rehab, specialist outpatient review, and the back-of-house epilogue (cath cleaner, ward cleaner, hospital cook, laundry coordinator, billing clerk, HCA day-in-the-life). 18 canonical chapters + 3 off-canonical (private A&E, secondary transfer, surgeon-decline) wired into the v9.4 engine without engine changes. 30+ Singapore-grounded actors. | ✅ shipped |
| **v9.6** | **Generated character sprites via SpriteForge Claude Skill**. Per-actor stylised SVG sprites replace the bare colour circles in the Stage. Skin tone, hair colour, hair style and accessory are derived deterministically from the actor id (FNV-1a hash). 15 accessory kinds (stethoscope, scrubs, lead apron with `Pb` marker, sterile gown, white coat, chef's hat, mop, linen cart, clipboard, AHP polo, hospital gown, high-vis vest, forage cap, headphones, casual). Singapore-aware: 4-band skin palette. Claude Skill at `.claude/skills/spriteforge/SKILL.md` documents the workflow for adding a new actor without leaving the editor. | ✅ shipped |
| **v9.7** | **High-definition pixel-art sprites + 4-direction + 6-frame interaction loop + 4-frame walk cycle**. Integer-grid construction (rect-only primitives) with `image-rendering: pixelated` and `shapeRendering: crispEdges`. Direction-aware: N hides facial features, E/W mirror via SVG transform. Animation loops are universal (same 6-frame breath / 4-frame stride for every actor) so authoring stays cheap; beats can opt-in to walking. Stage ticks at 6 fps (150 ms). All 30+ STEMI actors animate when active. | ✅ shipped |
| **v9.8** | **Cinematic scene staging + posed/expressive characters (realism pass)**. Replaces the team-row grid with composed *environments* (`src/lib/scenery.tsx`): 11 hand-built scenes — hero `kopitiam` (three hawker stalls with signage, ceiling fans, fluorescent tubes, marble tables + red stools, seated patrons, drink-stall queue, gathering onlooker ring, shophouse pillars, warm morning light) plus `street`, `resus`, `cathlab`, `imaging`, `counsel`, `ward`, `pharmacy`, `rehab`, `clinic`, `backhouse` — each with a perspective floor, props, lighting, ambient crowd, vignette and SMIL micro-animation (fans, steam, monitor traces, light flicker). Sprites gain **7 poses** (stand/walk/kneel/sit/cpr/collapsed/point) and **7 expressions** (neutral/alarmed/distressed/pained/focused/relieved/unconscious) with volume shading. The collapse chapter is fully choreographed — Mr Tan collapses, a bystander kneels into hands-only CPR, the attendant waves the crowd back, the myResponder CFR sprints in and delivers an AED shock. Beats carry `scene` / `pos` / `pose` / `expression`; figures depth-scale (0.7→1.3) and cast ground shadows; a single "current line" speech bubble tracks the lead beat. Size deliberately not optimised. | ✅ shipped |
| **v9.8.1** | **Readability + composition refinement** (live-device feedback). Speech bubble moved off the figures into a top "broadcast" band — it can no longer occlude characters — linked to the speaker by a faint dashed stem + head marker; the speaker is emphasised with a soft focus halo and a pulsing ring, and their floor name-tag is dropped (named in the bubble). Figures enlarged (base 30→34) for mobile legibility; default staging moved into the clear front band, centred right-of-middle to clear left-side fixtures. Street scene gains a wheeled stretcher so the patient isn't on bare tarmac; ambulance beats re-blocked (patient + paramedic action left-centre, driver bridging to the EA). | ✅ shipped |
| **v9.8.2** | **Patient staged in every clinical scene.** Mr Tan was missing as a visible figure in most clinical chapters (only collapse / ambulance / complications showed him). He's now placed in-scene throughout: lying on the resus trolley (arrive-SGH, private A&E), on the cath table under the C-arm (activation, diagnostic, PCI), sliding into the CT/MRI bore (CTA, MRI), settled in the CCU bed, propped up in the ward bed (day-2 round), on the secondary-transfer stretcher, on the treadmill (rehab), and on the clinic exam couch — with pose + expression tracking his condition (pained → neutral → relieved). | ✅ shipped |
| **v9.9** | **Phaser canvas renderer (beta)** — behind a `2D / Cinematic` toggle in the modal header; the default SVG stage is untouched. Reuses the hand-built SVG environments as a single full-stage canvas texture (so the backdrop is pixel-identical), then adds what a game loop does best: figures tween smoothly between beats, idle-bob at 60 fps, the speaker gets a pulsing focus ring, particle ambience drifts through the scene, and the camera shakes on the AED shock. Figures use the same hash-derived skin/uniform colours as the SVG sprites. A new pure module (`walkthrough-staging.ts`) is the single source of staging geometry for **both** renderers. Lazy-loaded (Phaser stays out of the default bundle) and wrapped in an error boundary that falls back to 2D. | ✅ shipped (beta) |
| **v9.10** | **Cinematic figure fidelity + walk-in paths.** The Phaser canvas now renders the *real* `ActorSprite` art — exact accessories, poses, expressions — instead of simplified Graphics avatars. New `spriteTexture.ts` bakes each (actor, pose, expression, direction) combination to a UTF-8 SVG data URI on first appearance, then Phaser caches it as a texture; the coordinate contract guarantees feet land exactly where the SVG stage places them (verified by ground-line overlay). Figures now **walk in** from the edge they face away from when the beat marks them walking (450ms glide for non-walking position changes; 850ms walk between beats; 950ms entry walk-in), with a brisker bob + slight tilt during motion. The figure label is suppressed on the speaker (already named in the bubble); exit animation slides departed actors off-screen. Pose textures are swapped seamlessly when a beat changes the pose. | ✅ shipped |
| **v9.11** | **Procedural "b-roll" showpieces (first hybrid content).** Beats can now declare a `showpiece` — either an inline SVG/SMIL animation (asset-free, ships with the bundle) or an external MP4 (content owners opt in by URL; the bundle stays asset-free). Three inline showpieces shipped: **stent-deployment** (vessel cross-section with stenosis → guidewire across → balloon inflation → DES struts deployed → TIMI 3 flow restored with travelling pulses), **mri-bore-slide** (side-view 1.5 T scanner → patient table advances into the bore → green scan-sweep + emanating pulse rings during acquisition → table retracts), **aed-shock** (torso with RA + APEX pad placement → biphasic arc fires during the shock window → VF trace converts to sinus on the strip). Each runs a 6–10s loop with a labelled phase progress bar. New `<ShowpieceOverlay>` floats above the stage as a 16:9 framed panel with a gold title bar + caption + soft scrim; fades in/out so the cut reads as film-grammar B-roll, not a jump-cut. MP4 failures fall back silently to no overlay so the stage action continues. Wired into the STEMI walkthrough at the three peak procedural moments (AED shock, MRI sequences, stent deployment). Bonus: denser kopitiam crowd (extra onlooker ring, additional back-row patrons) to push the busy-shop feel further. | ✅ shipped |
| **v9.15** | **Character distinctness pass + MRT seat alignment fix.** Live-device feedback: Mdm Lim was rendered "sitting in mid-air" because her sit beat anchor (y=230, foreground walkway) didn't match the existing MRT seats (y≈140, far-side back row). Composition fix: a new **foreground priority-seat bench** added to the MRT scene at the door (purple cushion with "RESERVED · 优先座" decal, glass armrest, anti-slip ridges, ♿ sticker on the back rest) sized at full stage scale so the action of easing the patient onto it reads as a hero shot, not a back-row distant figure. Mdm Lim's beats re-blocked at y=242 so her hips land on the cushion. Character generation pushed: `deriveFeatures` now derives an `ageBand` (young / adult / elder) from explicit role-text age ("Mdm Lim, 72" → elder) or hash-jittered role keywords; elders pull from a new gray + white end of the `HAIR_COLOURS` palette (~75% by hash), render crow's-foot wrinkles at the temples, and are biased toward glasses. New `hasGlasses` field (rounded twin frames with sheen reflection + temple arms, biased high for elders + clinical roles), new `beard` field (none / light stubble / full, suppressed entirely for female-coded explicit roles `Mdm`/`Mrs`/`Ms`/`Miss`/`Daughter`/`Wife` and for tied-back hair). Mdm Lim now reads as 72 (gray hair + glasses + wrinkles), the consultant cardiologist as a senior doc (beard), the MO as a junior (young, dark hair), Mr Tan as middle-aged — the cast feels demographically real instead of generic. | ✅ shipped |
| **v9.14** | **Assets-heavy ultra-realism pivot + new MRT scene.** Project's prior "asset-free PWA" rule retired on the user's instruction. New `scripts/bake-scenes.mjs` (run via `pnpm bake:scenes`, powered by `@resvg/resvg-js` + `vite-node`) pre-renders every walkthrough scene as a 1440×810 PNG to `public/walkthrough/scenes/{id}.png` (~15 MB of binary assets committed). Both renderers — SVG `<Stage>` (via a new `<UltraBackground>` `<image>` element) and Phaser canvas (via the loader → texture path) — now load the HD PNG as the backdrop by default with a graceful inline-SVG fallback if the asset is missing (dev without `pnpm bake:scenes`, or a PWA cache miss). New `<CinematicOverlay>` defaults-on in every scene: a feTurbulence film-grain layer (alpha 0.05, seeded turbulence), a corner-vignette radial gradient, and a warm-shadow colour-grade tint. **New `mrt` scene**: assets-heavy SVG of the inside of a Singapore MRT carriage in motion — ceiling fluorescent strips with bloom, the EWL route diagram (Bedok highlighted), trilingual "NEXT: TAMPINES" LED indicator, sliding doors with SMRT decals + red SOS plunger, the red-and-blue priority + standard seat upholstery with anti-slip texture, brushed-aluminium grab poles, swaying hand straps, yellow tactile strip + stand-back line, no-eating/no-smoking pictograms, motion-blurred tunnel scenery through the windows (feGaussianBlur), and silhouettes of seated commuters. Stroke walkthrough opening rewritten: NOT tai-chi in the void deck — Mdm Lim is on the East-West Line between Bedok and Tampines stations during the morning commute when right-side weakness + slurred speech hit; fellow commuter (new `bystander` role) catches her, presses the SOS plunger; new actor `smrt-staff` (Tampines station manager) responds, calls 995, hands over emergency-contact info to paramedics at the platform. | ✅ shipped |
| **v9.13.1** | **SVG renderer motion parity with Phaser.** Figures in the default SVG `<Stage>` now glide between beat positions (CSS transform transitions: 450ms ease for normal repositions, 850ms for `walking: true` beats) instead of snapping. Brings SVG mode in line with the Phaser canvas's tweened motion — same easing curve (cubic-bezier 0.4, 0, 0.2, 1), same walking-vs-glide distinction. No animation on first paint of a chapter (the figures appear in place), only on subsequent beat-driven position changes within a chapter. | ✅ shipped |
| **v9.13** | **Second walkthrough — stroke (LVO) pathway.** New `walkthrough-stroke.ts` ships a 12-chapter cinematic of Mdm Lim, 72: sudden onset right-hemiparesis + aphasia during morning tai-chi at the HDB void deck → 995 → SCDF FAST screen + stroke-bypass → NNI@TTSH hyperacute stroke unit → CT head excludes ICH → CTA shows left M1 occlusion + CTP mismatch → **decision branch**: bridging IV thrombolysis + mechanical thrombectomy (canonical) vs primary thrombectomy alone (off-canonical: tPA contraindicated) → alteplase bolus + infusion → endovascular thrombectomy with stent retriever + aspiration → neuro-ICU 24h monitoring → stroke ward MDT (neurology + nurse + SLT + physio + OT) → **rehab branch**: community-hospital inpatient block (canonical) vs home-with-outpatient → 6-week stroke-clinic review with secondary-prevention plan. 18 actors with distinct deterministic sprites (each hashed off a stroke-specific id so the cast doesn't collide with the STEMI cast). New `thrombectomy-pass` showpiece — slightly arched MCA cross-section with dark M1 clot, four-phase animation (guide → stent-retriever deployed across clot → 4-min engagement → withdrawal *with* the clot → restored flow), distinct from the cardiac stent-deployment showpiece (which leaves the stent behind). Launcher in TrendsPanel converted from a single button to a picker with **STEMI** and **Stroke (LVO)** siblings; each walkthrough is a separate lazy chunk so only the chosen one enters the bundle. | ✅ shipped |
| **v9.16** | **Award-game polish quartet.** (1) **Lip-sync** — the currently-speaking lead actor's mouth animates (SMIL height/y pulse on the pixel-art mouth) in both renderers; `speaking` threads through the Phaser sprite-texture cache key; suppressed when back-facing. (2) **SFX text indicators** — beats can declare `sfx` (onomatopoeia); a floating glyph rises and fades above the speaker (⚡ CLEAR! on the AED shock, 🚨 SIREN on the ambulance load, 🔔 DING! on the MRT SOS plunger, 📻 *kssh* on the SMRT radio). The PWA stays audio-asset-free but actions gain a sound dimension. (3) **Walkthrough achievements** — `walk-stemi`, `walk-stroke` on terminal-chapter completion plus `walk-multi-pathway` once both are held (fires in the same pass when completing the second). (4) **Letterbox bars** — 8%-height black bars slide in from the stage edges whenever a B-roll showpiece is active; film-grammar widescreen cut. | ✅ shipped |
| **v9.17.3** | **Animation retargeting scaffold + PBR material upgrades.** New `src/game3d/animationLibrary.ts`: every `BeatPose` maps to a clip slug (`idle-breathing`, `walking`, `kneeling`, `sitting`, `cpr-compressions`, `lying-down`, `pointing`); `PoseAnimationDriver` wraps a per-figure `AnimationMixer`, auto-loads clips from `public/3d/anims/{slug}.glb` (Mixamo-naming-compatible) on demand, and cross-fades between actions on pose change. Wired into the GLB-backed figure path — the moment any standard-humanoid clip lands in `public/3d/anims/` it just works; until then every API is a safe no-op. `cloneRig` (thin wrapper over `SkeletonUtils.clone`) preserves the skinned skeleton across instances. PBR material upgrades to the surfaces that genuinely require physical lighting to read correctly: **clearcoat metal** on the cath-lab C-arm + detector + tube and on the MRI gantry shell (real specular highlights from IBL); **transmission glass** on the cath-lab lead-glass control window, the MRI control window, and the ward daylight window pane (real refraction + soft warm bleed-through under IBL HDRIs). 5 new tests (710 total). Bundle: 3D base chunk 662 kB (+2 kB for `SkeletonUtils`); lazy postFx chunk 174 kB. | ✅ shipped |
| **v9.17.2** | **Photoreal asset pipeline — infrastructure-only landing.** Three new modules under `src/game3d/`: (1) `actorLoader.ts` — async GLTF actor cache that tries `public/3d/cast/{actorId}.glb` per actor and *swaps the procedural rig in place* when the GLB resolves, while keeping the procedural `Humanoid` as the synchronous fallback (so the offline-PWA shape and empty-asset-folder dev experience both still work); (2) `ibl.ts` — per-scene HDR envmap loader using `RGBELoader` + `PMREMGenerator` to set `scene.environment` for realistic PBR specular + ambient response, with a `SCENE_HDRI_SLUGS` registry mapping each of the 12 scenes to a Poly Haven HDRI; (3) `postFx.ts` — `EffectComposer` chain (SSAO + bloom + vignette + SMAA) **lazy-imported** on first PostFX toggle so the base 3D chunk stays lean. PostFX toggle button added to the 3D stage corner. New `scripts/fetch-3d-assets.mjs` (run as `pnpm fetch:3d`) auto-downloads CC0 HDRIs (Poly Haven), CC0 rigged characters (Quaternius Ultimate Animated Character Pack + Medical Pack), and CC0 props (Kenney Platformer Characters) into `public/3d/{hdr,cast,props}/` — the entire asset folder is `.gitignore`d so binary blobs never enter the repo. 7 new headless tests (705 total). Bundle: base 3D chunk 660 kB (was 536 kB — extra ~120 kB is GLTFLoader + RGBELoader); lazy postFx chunk 174 kB. **Handoff status**: infrastructure shipped; you (the user) run `pnpm fetch:3d` once on your machine to populate `public/3d/` because the sandbox blocks external CDNs. Subsequent asset authoring (MakeHuman / Mixamo / Marvelous Designer) needs desktop GUIs documented in `docs/3D-RENDERER.md`. | ✅ shipped |
| **v9.17.1** | **3D framing + facing fix.** Live-device feedback: figures were dot-sized (world too wide for the camera) and W/E facing was swapped (attendant + CFR pointing the wrong way). `space.ts` tightened scale (`1/24` X, `1/14` Z → 20 m × 12.9 m world), narrower FOV 32°, closer camera (z=8.5, y=3.6); shadow frustum re-tuned; kopitiam + street set-dressing re-positioned to sit inside the new field of view. `yawFor` corrected so E maps to +π/2 (stage-right) and W to −π/2 (stage-left), with a dedicated regression test asserting `sin(yawFor('E')) === 1`. Diff: a 1.8 m figure at front-row now occupies ~33% of frame height (was ~5%). New `docs/3D-RENDERER.md` documents the Babylon.js vs Three.js decision and lists CC0/free rigged-character sources (Mixamo, RPM, MakeHuman/MB-Lab, Sketchfab CC0, Poly Haven HDRIs) for the future photoreal asset swap-in. | ✅ shipped |
| **v9.17** | **Three.js 3D renderer (beta)** — third option in the renderer toggle (2D / Cinematic / 3D ᴮᴱᵀᴬ). True-3D WebGL stage consuming the SAME `walkthrough-staging.ts` frames as the other two renderers: `src/game3d/space.ts` maps stage units → world metres (perspective replaces the `depthScale` hack), `environments.ts` rebuilds all 12 scenes as procedural 3D sets with per-scene lighting moods (warm kopitiam with spinning ceiling fans + glowing stall signs, street with blinking SCDF ambulance beacon + lit HDB facade, MRT carriage with moving tunnel light-streaks + the v9.15 priority bench, resus/cathlab C-arm/imaging gantry/counsel/ward/pharmacy/rehab/clinic/backhouse), `humanoid.ts` builds articulated characters from the SAME `deriveFeatures` hash as the 2D sprites (skin/hair/age/glasses/beard/stethoscope/white coat/lead apron/chef hat/hi-vis all carry over) with pose-blend animation: walk cycles, kneel/sit, CPR compressions at ~110/min, collapse, lip-sync jaw, geometric facial expressions, breathing idle. ACES filmic tone mapping, PCF-soft shadows, exponential fog, speaker-tracking camera dolly + shock-beat shake, raycast actor picking. DOM overlay carries the caption band + SFX indicator (crisper than canvas text). Lazy 536 kB chunk (gzip 139 kB) loaded only on opt-in; error boundary falls back to 2D. 29 headless unit tests (space mapping, all 12 environment builders, rig/pose/animation). | ✅ shipped (beta) |
| v9.12 | Locale translation for the walkthrough strings (`zh`, then `ms`, then `ta`) — held pending native-speaker review per the project's clinical-content quality bar. Plumbing prepared for v10. | held |
| **v9.18** | **Asset-population handoff completed + backlog quartet.** (1) `pnpm fetch:3d` ran end-to-end on the target machine: 12/12 Poly Haven HDRIs (counsel re-slugged `industrial_room` → `hospital_room` after a 404), Kenney pack URL refreshed; Quaternius direct-zip URLs were dead → pack now pulled from their CC0 Google Drive folder via `gdown`, and a new `pnpm map:cast` (`scripts/map-quaternius-cast.mjs`, `@gltf-transform/core`) converts the 52-character glTF pack to GLB: 14 role-shared characters into `public/3d/cast/_lib/` + the 7 `BeatPose` clips extracted into `public/3d/anims/` from the pack's own animation set (no Mixamo login needed). New `castManifest.ts` maps ANY walkthrough actor to a role-appropriate library character (override `cast/{actorId}.glb` still wins; procedural rig remains the final fallback). (2) **Third walkthrough — sepsis**: community urosepsis pathway (onset → SCDF qSOFA pre-alert → SSC Hour-1 bundle in resus → CT source hunt → emergency decompression of an obstructed infected kidney → ICU vasopressors → ward de-escalation → post-sepsis clinic → back-of-house micro lab/pharmacy/portering), 3 decision branches, 22 actors, `walk-sepsis` achievement, multi-pathway now requires all three. (3) **Singlish-aware glossary toggle** (Settings): 14 colloquial terms ("kopi", "void deck", "tahan") folded into the glossary scanner, off by default. (4) **ms/ta narration degradation**: `getVoiceSupport()` capability check — when the platform has no voice for the active locale the narration reads the English fallback text instead of mangling ms/ta through a wrong-language voice, with an "EN voice" hint. (5) **WebGPU opt-in (β)**: capability check + persisted flag + `Stage3D.create()` async factory that renders via `three/webgpu` (lazy 644 kB chunk) and falls back to WebGL on any failure; PostFX + PMREM IBL stay WebGL-only by design. (6) Three.js + postprocessing pinned to exact versions (`0.184.0` / `6.39.1`). 769 tests. | ✅ shipped |

### Renderer / engine decision (why Phaser, not Unity)

A Unity MCP server (`unity-mcp-server`) was evaluated for driving the cinematic.
Decision: **stay on Phaser, within the web stack.** Reasons:

- **Offline, asset-free PWA is a hard product constraint.** A Unity WebGL build
  ships a multi-MB WASM + data bundle and an external player; the whole project
  is deliberately inline-SVG / procedural so it installs and runs offline with
  no binary downloads. Unity breaks that promise.
- **One stack, one test surface.** The renderer shares React state, i18n, the
  Vitest suite, and the `walkthrough-staging.ts` geometry. Phaser is already a
  dependency (the ops/hospital canvas). Unity would be a parallel C#/editor
  toolchain with no path into the existing tests or CI.
- **No Unity MCP is connected to this environment** in any case (only a generic
  Three.js scene tool is available). If photoreal 3D is wanted later, the
  lighter path is a Three.js/WebGL layer behind the same renderer toggle —
  same data, same constraints — rather than Unity.

## Resumed after Walkthrough — Multilingual case content

| Version | Deliverable | Status |
| - | - | - |
| v10.1 | Case-content translation track — `ms` for primary-care / public-health cases first. **First slice shipped** (machine-assisted, flagged): `paeds-vaccine-hesitancy` + `agewell-hpc` fully carry `ms`; coverage-floor test ratchets the count up only. | 🔄 in progress |
| v10.2 | Case-content translation track — `ta`, same case ordering. **First slice shipped** (same two cases, same flagging). | 🔄 in progress |
| **v11.0** | Capstone: all four official languages at full UI + case parity; mother-tongue first-run suggestion from `navigator.languages`; "end-to-end translation" claim restored for all locales. **Infrastructure shipped**: `case-locale-coverage.ts` measures real per-locale case coverage (finding: zh sits at ~93.7% of strings, not the advertised 100% — now floored by test); the language switcher shows per-locale case % + a machine-assisted/pending-review hint; `<LanguageSuggestion>` offers a one-tap mother-tongue switch on first run from `navigator.languages`. Remaining: the content itself + native review. | 🔄 infra shipped |

### Parity definition (the bar each locale must clear)

1. **UI chrome**: 100% of `en` keys present (measured by `parity.test.ts`).
2. **Critical keys**: every navigation / entry-point / verdict key present
   (measured by `coverage.test.ts`).
3. **Case content**: every case's `title`, `blurb`, node framing, decision
   prompts, option labels, rationales, outcomes, and guideline references carry
   the locale, **and have passed native-speaker review** before the locale is
   advertised as complete.

### Quality / responsibility note

Malay and Tamil clinical strings added by this track are machine-assisted and
**must be reviewed by native clinical speakers** before the "fully translated"
claim is extended to them. The roadmap advertises *coverage*, not *certified
translation quality*, until that review lands. This is called out in the
language switcher (partial-coverage hint) so learners are never misled.

## Backlog (post-v10, unscheduled)

- ~~Per-case audio narration for `ms`/`ta` graceful-degradation check~~ — ✅ shipped in v9.18 (`getVoiceSupport()` + English-text fallback + switcher hint).
- ~~Singlish-aware glossary toggle~~ — ✅ shipped in v9.18 (Settings toggle, 14 terms, off by default).
- ~~Third walkthrough (sepsis)~~ — ✅ shipped in v9.18.
- ~~WebGPU renderer opt-in~~ — ✅ shipped in v9.18 (capability check + flag + fallback).
- Real-time GI (v9.19+ if needed) — revisit only if PostFX-on still looks flat
  now that authored assets have landed.
- Close the remaining zh case-content gap (~6% of strings) surfaced by the
  v11.0 coverage meter.
